$("#comments_area").html("<%= j(render 'comments/index', {comments: @comment.video.comments.includes(:system_admin, :user, :viewer, :replies).order(created_at: :desc)}) %>");
$("textarea").val('');
$('#flash-message').html("<%= j(render 'layouts/flash') %>");
