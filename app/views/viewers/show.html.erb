<% provide(:title, "視聴者詳細") %>

<div class="card">
  <div class="card-body table-responsive p-0">
    <table class="table table-hover text-nowrap">
      <tbody>
        <tr>
          <th><%= "名前" %></th>
          <td>
            <%= @viewer.name %>
            <% if @viewer.is_valid == false %>(退会)<% end %>
          </td>
        </tr>
        <tr>
          <th><%= "メールアドレス" %></th>
          <td><%= @viewer.email %></td>
        </tr>
        <% @organizations.each.with_index(1) do |organization, i| %>
        <tr>
          <th><%= "所属組織名#{i}" %></th>
          <td><%= link_to organization.name, organization_path(organization) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="card-footer">
  <% if current_viewer&.id == params[:id].to_i %>
    <%= link_to "編集", edit_viewer_path, class: "btn btn-md btn-block btn-success" %>
    <%= link_to "退会ページ", viewers_unsubscribe_path(@viewer), class: "btn btn-md btn-block btn-danger" %><br>
  <% end %>
  </div>
</div>